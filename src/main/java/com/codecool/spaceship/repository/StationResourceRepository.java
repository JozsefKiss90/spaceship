package com.codecool.spaceship.repository;

import com.codecool.spaceship.model.resource.StationResource;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StationResourceRepository extends JpaRepository<StationResource, Long> {
}
